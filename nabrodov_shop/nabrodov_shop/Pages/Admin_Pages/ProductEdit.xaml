<Page x:Class="nabrodov_shop.Pages.Admin_Pages.ProductEdit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:nabrodov_shop.Pages.Admin_Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductEdit">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="AliceBlue">
            <ScrollViewer>
                <StackPanel x:Name="Form" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Название продукта" FontSize="16" Margin="10"/>
                    <TextBox Text="{Binding ProductName}" FontSize="20" Width="200" Margin="10"/>
                    <TextBlock Text="Стоимость" FontSize="16" Margin="10"/>
                    <TextBox PreviewTextInput="NumberValidation" PreviewKeyDown="NoSpace" Text="{Binding Price}" FontSize="20" Width="200" Margin="10"/>
                    <TextBlock Text="Количество" FontSize="16" Margin="10"/>
                    <TextBox PreviewTextInput="NumberValidation" PreviewKeyDown="NoSpace" Text="{Binding Quantity}" FontSize="20" Width="200" Margin="10"/>
                    <TextBlock Text="Категория" FontSize="16" Margin="10"/>
                    <ComboBox Name="CategoryCBox" SelectedItem="{Binding Categories}" DisplayMemberPath="CategoryName" FontSize="20" Width="200" Margin="10"/>
                    <WrapPanel>
                        <Button Content="Сохранить" FontSize="14" Padding="8" Width="90" Margin="10" HorizontalAlignment="Left" Click="SaveProduct_Click"/>
                        <Button Content="Удалить" FontSize="14" Padding="8" Width="90" Margin="10" HorizontalAlignment="Right" Click="DeleteProduct_Click"/>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        
        <DataGrid Grid.Column="1" x:Name="ProductDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID продукта" Binding="{Binding ProductId}" Width="*"/>
                <DataGridTextColumn Header="Название продукта" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Стоимость" Binding="{Binding Price}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Categories.CategoryName}" Width="*"/>
                <DataGridTemplateColumn Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="EditProduct_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
