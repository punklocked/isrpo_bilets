<Page x:Class="Nabrodov_WPF2.Pages.GenresPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Nabrodov_WPF2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GenresPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="AliceBlue">
            <StackPanel x:Name="Form" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Название жанра" FontSize="16" Margin="10"/>
                <TextBox Text="{Binding Name}" FontSize="20" Width="200" Margin="10"/>
                <WrapPanel>
                    <Button Content="Сохранить" Click="AddGenre_Click" FontSize="14" Padding="8" Width="90" Margin="10" HorizontalAlignment="Left"/>
                    <Button Content="Удалить" Click="DeleteGenre_Click" FontSize="14" Padding="8" Width="90" Margin="10" HorizontalAlignment="Right"/>
                </WrapPanel>
            </StackPanel>
        </Grid>
        <DataGrid Grid.Column="1" x:Name="GenresDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название жанра" Binding="{Binding Name}" Width="*"/>
                <DataGridTemplateColumn Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="EditGenre_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
